#include "main.h"

/**
 * int _islower(int c) - checks for lowercase character
 *
 * Return: 1  if lowercase and 0 if not
 */

int _islower(int c)
{
	int c;

	if (c = 'a' && c = 'z')
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
