#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 success
 * @argc: Returns an argument count
 * @argv: Returns a sttring of characters to main
 */

int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);

	return (0);
}
