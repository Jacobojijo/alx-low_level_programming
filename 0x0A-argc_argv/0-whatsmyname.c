#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 success
 * @argc: Returns an argument count
 * @argv: Returns a string of characters to main
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", *argv);

	return (0);
}
